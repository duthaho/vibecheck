{
  "name": "vibecheck",
  "version": "0.1.0",
  "description": "Keyless quality canary for Claude Code — is YOUR Claude Code degraded? Answered with data, not vibes.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "vibecheck": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.226"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
